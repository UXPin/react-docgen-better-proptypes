import { Props } from 'react-docgen';

export const colorPropsResult:Props = {
  color: {
    description: 'This is color prop description',
    required: true,
    type: {
      name: 'string',
    },
  },
};
